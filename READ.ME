категорії приведи до реальних із бази у форматі


Redis

1)Перевірити, чи Redis встановлений

bash - "redis-server --version"  //// інсталювати ->"sudo apt update" потім "sudo apt install redis-server"
MacOS - "brew install redis"

2)Запустити Redis
"redis-server"
3)Перевірити статус
"redis-cli ping" Має відповісти: PONG.
